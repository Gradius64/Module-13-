
    /// Add Candidates in list

   

    let addCandidates = [];
    const listCandidates = localStorage.getItem('listCandidates');
    if (typeof listCandidates === 'string') {

        const list: string[] = ["Candidate1", "Candidate2"];

        interface Candidate {
            name: string;
            value: number;
       }

       const Quantity: Candidate[] = [
            { name: "William", value: 26 },
            {name: "Zachary", value: 54 },
            

       ];

       

       const Area = localStorage.getItem('CandidateNumber');
       if (typeof CandidateSearch === 'string') {

        let Candidate = [2, 4, 'TypeA', 'TypeB'];

       }


    
    

    

























  

}
 
